#!/usr/bin/env direnv
# Development environment for skills-akaihola project
# Automatically syncs dependencies and activates Python virtual environment

set -e

# Create and activate virtual environment
uv sync

# Activate the environment
PATH_add .venv/bin
export VIRTUAL_ENV="$(pwd)/.venv"
